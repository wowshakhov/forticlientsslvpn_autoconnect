# forticlientsslvpn_autoconnect

## Usage

`./forticlientsslvpn_autoconnect` - runs VPN with config from config.json (default)

`./forticlientsslvpn_autoconnect config_name.json` - runs VPN with config from config_name.json
